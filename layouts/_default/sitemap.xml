{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">
  {{- range .Data.Pages }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>
    {{- end }}
    {{- if or (eq .RelPermalink "/weddings/") (eq .RelPermalink "/") }}
    {{- range $.Site.Data.gallery }}
    {{- $img := resources.Get (printf "images/%s" .src) }}
    {{- if $img }}
    {{- $processed := $img.Resize "800x webp q90" }}
    <image:image>
      <image:loc>{{ $processed.Permalink }}</image:loc>
      <image:caption>{{ .alt | htmlEscape }}</image:caption>
    </image:image>
    {{- end }}
    {{- end }}
    {{- end }}
  </url>
  {{- end }}
</urlset>
